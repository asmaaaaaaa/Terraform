# Terraform demo
# so here we create s3 and ec2 instance using terraform automate tool
# we need to create an EC2 instance and get the ami ID to configure the new instance that we want to create with this ami ID
# choose a unique name for s3 bucket
# add credentials to guthub secret repository for aws platform
